const FLAGS_DEFAULT = {
	ENABLE_CERTIFICATION: true,
	ENABLE_CALENDAR: true,
	ENABLE_INVITE: true,
};

export const DEV_FLAGS = {
	...FLAGS_DEFAULT,
};

export const PROD_FLAGS = {
	...FLAGS_DEFAULT,
};
